namespace JoyTeam
{
    public enum SortingLayers
    {
        Default = 0,
        Game,
        UI,
        Selected
    }

    public enum Layers
    {
        Default = 0,
        TransparentFX,
        IgnoreRaycast,
        Game,
        Water,
        UI
    }
}